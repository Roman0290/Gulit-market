# Gulit Market

A mobile marketplace connecting local vendors and customers for everyday essentials.

- `pocket-market-api/` — Go + Gin backend (PostgreSQL, JWT auth, Stripe payments)
- `pocket-market-app/` — Flutter frontend
